#include "texture_2d.h"
#include "device.h"

namespace d3d11 {
	//inline auto texture_2d::create_render_target_view(D3D11_RENDER_TARGET_VIEW_DESC* desc) const noexcept -> render_target_view {
	//	ID3D11RenderTargetView* view;
	//	device::instance().data()->CreateRenderTargetView(_component, desc, &view);
	//	return render_target_view(view);
	//}
	//inline auto texture_2d::create_shader_resource_view(D3D11_SHADER_RESOURCE_VIEW_DESC* desc) const noexcept -> shader_resource_view {
	//	ID3D11ShaderResourceView* view;
	//	device::instance().data()->CreateShaderResourceView(_component, desc, &view);
	//	return shader_resource_view(view);
	//}
	//inline auto texture_2d::create_unorder_access_view(D3D11_UNORDERED_ACCESS_VIEW_DESC* desc) const noexcept -> unorder_access_view {
	//	ID3D11UnorderedAccessView* view;
	//	device::instance().data()->CreateUnorderedAccessView(_component, desc, &view);
	//	return unorder_access_view(view);
	//}
	//inline auto texture_2d::create_depth_stencil_view(D3D11_DEPTH_STENCIL_VIEW_DESC* desc) const noexcept -> depth_stencil_view {
	//	ID3D11DepthStencilView* view;
	//	device::instance().data()->CreateDepthStencilView(_component, desc, &view);
	//	return depth_stencil_view(view);
	//}
}